/*
Corrige el código para que el resultado de la variable result sea igual al valor retornado 
al llamar a la función getNine.
*/

function getNine() {
  let x = 6;
  let y = 3;
  return x + y;
}

let result = getNine;
console.log(result);